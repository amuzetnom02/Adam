specVersion: "v1"
serviceId: "adam"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "adam-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
